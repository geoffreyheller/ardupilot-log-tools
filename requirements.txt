# Core analysis. Any reasonably recent version works; these are the floors tested against.
# Python 3.9+ on Windows, Linux and macOS. `pip install -r requirements.txt`, or
# `pip install -e .` for the `alog` console script, or `uv sync` / `uv run alog`.
numpy>=1.22
pandas>=1.4
scipy>=1.8

# Charts only (alog fft --plot, plot_notch.py). The CLI works without it.
matplotlib>=3.5
